[user]
	name = Matthieu Gicquel
	email = matthieug@hey.com
[filter "lfs"]
	clean = git-lfs clean -- %f
	smudge = git-lfs smudge -- %f
	process = git-lfs filter-process
	required = true
[credential]
	helper = osxkeychain
[diff]
	algorithm = histogram
